# 🎵 Music Streaming App – Backend

This is the backend server for the Music Streaming Application.
It provides REST APIs for songs, playlists, podcasts, and authentication using **Node.js, Express, and Supabase**.

---

## 🚀 Tech Stack

- Node.js
- Express.js
- Supabase (PostgreSQL + Auth)
- Axios
- JWT Authentication
- Jamendo API (for demo songs)
- Nodemon

---


## 🔐 Environment Variables

Create a `.env` file in the root directory:

```env
PORT=5000
SUPABASE_URL=your_supabase_url
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
JAMENDO_CLIENT_ID=your_jamendo_client_id
JWT_SECRET=your_jwt_secret

```


▶️ Run the Server
  
```

npm install
npm run dev

```
## Server runs on:

http://localhost:5000







